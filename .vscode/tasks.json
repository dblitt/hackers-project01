{
    "tasks": [
        {
            "type": "shell",
            "label": "Make sysmonitor",
            "command": "/usr/bin/make",
            "args": [
                "--always-make",
                "sysmonitor"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": ["relative", "${workspaceFolder}"]
            },
            "group": {
                "kind": "build"
            }
        }
    ],
    "version": "2.0.0"
}
